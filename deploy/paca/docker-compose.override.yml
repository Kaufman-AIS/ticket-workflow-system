# Used with the official release compose from:
# https://github.com/Paca-AI/paca/releases/latest/download/docker-compose.yml
#
# Release v0.16+: gateway service is `gateway` (Caddy image).
# Suppress the built-in agent with: --scale agent-runner=0
services:
  gateway:
    ports: !override
      - "127.0.0.1:8090:80"
